[CLI]
# Default scene to render
scene_names = HardyCrossScene

# Output directory (relative to this file)
media_dir = ./results
video_dir = ./results/videos/scenes/

# Background color
background_color = WHITE

# Render settings
frame_rate = 60
pixel_height = 1080
pixel_width = 1920
renderer = cairo

# Frame dimensions (canvas size in Manim units)
frame_height = 40
frame_width = 40

